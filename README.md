# MatrixLinkedList Vanilla-Java
MatrixLinkedList Project Assignment in College. Every element of the matrix is represented in a MatrixNode that has a variable of type integer called data, and two references to MatrixNodes called right and down as illustrated in the below figure.
